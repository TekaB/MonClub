<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{{ page_title }}{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
        {% endblock %}

        {% block javascripts %}
            {{ importmap() }}
        {% endblock %}
    </head>
    <body>
        {% include 'flashes/all.html.twig' %}
        <nav class="navbar navbar-expand-lg bg-light navbar-light mb-3">
            <a class="navbar-brand text-primary" href="{{ path('app_club') }}">MonClub</a>
            <div class="collaspe navbar-collapse">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item d-flex align-items-center">
                        <img src="{{ '/uploads/club_images_directory/' ~ club.club.image }}" width="25px" alt=""><a class="nav-link text-info" href="{{ path('app_club') }}">{{ club.getClub.nom }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_equipe') }}">Mes équipes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_joueur') }}">Mes joueurs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_rencontre') }}">Rencontres</a>
                    </li>
                </ul>
            </div>
        </nav>
        {% block content %}
        {% endblock %}
    </body>
</html>
